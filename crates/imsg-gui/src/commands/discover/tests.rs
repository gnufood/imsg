//! `discover_list_paired_devices`/`discover_resolve_channels` need a real `BlueZ` adapter and
//! real SDP exchange — not unit-tested here, same reasoning as `crate::discover` and
//! `imsg-transport::discover` (see those modules' own notes).
