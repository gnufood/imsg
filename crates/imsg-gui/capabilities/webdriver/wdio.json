{
  "$schema": "../../gen/schemas/desktop-schema.json",
  "identifier": "wdio",
  "description": "WebDriver testing support (@wdio/tauri-service, embedded provider) — only active behind the `webdriver` Cargo feature, see Cargo.toml/build.rs/main.rs.",
  "windows": ["main"],
  "permissions": ["wdio:default", "wdio-webdriver:default"]
}
